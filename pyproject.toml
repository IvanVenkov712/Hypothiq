[project]
name = "hypothiq"
version = "0.1.0"
dependencies = [
    "backtrader==1.9.78.123",
    "pandas",
    "PyYAML",
]

[project.optional-dependencies]
test = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["test"]
pythonpath = ["src"]
